#include "Scene.h"


namespace SpaRcle {
    namespace Graphics {
        bool Scene::Load(std::string name)
        {
            return false;
        }

        bool Scene::Close()
        {
            return false;
        }

        bool Scene::Create(std::string name)
        {
            return false;
        }

        bool Scene::Save()
        {
            return false;
        }
    }
}